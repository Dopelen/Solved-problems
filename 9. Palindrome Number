"""You can see the description of the task using the code specified in the title on letcode.
This program have O(n) complexity by time

I just really want one line here so sacrificed clarity. 
The reverse function returns us an iterator, so we need to convert it to a list and then convert it to a string for comparison.
"""

class Solution:
    def isPalindrome(self, x):
        return str(x) == "".join(list(reversed(str(x))))
