#!/usr/bin/python
"""You can see the description of the task using the code specified in the title on leetcode.
This program has O(n ** 2) complexity by time and O(1) by space.

For some reason, my brute force solution worked, although in the comments they wrote that it didn’t work.
"""

# Almost TLE Solution
class Solution:
    def findSolution(self, customfunction: 'CustomFunction', z: int) -> List[List[int]]:
        answer_list = []
        for x in range(1, 1000):
            for y in range(1, 1000):
                local_result = customfunction.f(x, y)
                if local_result == z:
                    answer_list.append([x, y])
        return answer_list

# Optimise solution
class Solution:
    def findSolution(self, customfunction: 'CustomFunction', z: int) -> List[List[int]]:
        x, y = 1, z
        pairs = []

        while x <= z and y > 0:
            cf = customfunction.f(x, y)
            if cf == z:
                pairs.append([x, y])
                x, y = x + 1, y - 1
            elif cf > z:
                y -= 1
            else:
                x += 1
        return pairs
