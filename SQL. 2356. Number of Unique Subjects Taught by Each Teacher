#!/usr/bin/python
"""You can see the description of the task using the code specified in the title on letcode.

Using DISTINCT inside group function!

"""

SELECT teacher_id, COUNT(DISTINCT subject_id) AS cnt
FROM Teacher
GROUP BY teacher_id
