#!/usr/bin/python
"""You can see the description of the task using the code specified in the title on letcode.

Now Im trying solve SQL tasks here too!"""

SELECT employee_id, if((employee_id % 2 <> 0 and Employees.name NOT LIKE 'M%'), salary, 0) as bonus
FROM Employees
ORDER BY employee_id
