#!/usr/bin/python
"""You can see the description of the task using the code specified in the title on leetcode. 
This programm has O(n) complexity by time and O(1) by space.

It’s not that I directly know Pandas, but knowing SQL and having 5 minutes to google basic operations, they can be solved in one line.
"""

import pandas as pd

def modifySalaryColumn(employees: pd.DataFrame) -> pd.DataFrame:
    employees.salary = 2*employees.salary
    return employees
